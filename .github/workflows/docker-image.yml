name: Docker Compose CI + Push to GHCR

on:
  push:
    branches:
      - Lab-7
  pull_request:
    branches:
      - Lab-7

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker images
        run: docker compose build

      - name: Tag Docker images with branch name
        run: |
          BRANCH_NAME="${GITHUB_REF##*/}"
          echo "Tagging images with branch name: $BRANCH_NAME"

          docker tag java_app ghcr.io/SantiagoSu15/Lab-5-ARSW/java-app:$BRANCH_NAME

      - name: Push Docker images to GHCR
        run: |
          BRANCH_NAME="${GITHUB_REF##*/}"
          echo "Pushing images to GHCR with branch name: $BRANCH_NAME"

          docker push ghcr.io/SantiagoSu15/Lab-5-ARSW/java-app:$BRANCH_NAME


      - name: Stop containers
        run: docker-compose down